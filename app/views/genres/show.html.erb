<h1>Gênero: </h1>
<h2><%= @genre.name %> </h2>

<%= link_to 'Editar', edit_genre_path%></br>
<%= link_to 'Voltar', genre_path %>

<div><strong>Nome:</strong> <%= @genre.name %> </div>


<h3>Filmes cadastrados de <%= @genre.name %>: </h4>
<ul>
<% @movies.each do |movie| %>
<li><%= movie.title %> </li>
<% end %>
</ul>
